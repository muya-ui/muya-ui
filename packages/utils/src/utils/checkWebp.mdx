---
name: checkWebp
menu: 工具方法
route: /utils/checkwebp
---

# checkWebp

检查是否可以使用 webp 格式的图片

Example:

```js
import { checkWebp } from '@muya-ui/utils';

async function someAction() {
  const canUseWebp = await checkWebp();
  // true or false
}
```
