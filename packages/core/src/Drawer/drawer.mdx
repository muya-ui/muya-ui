---
name: Drawer 抽屉
menu: 组件
route: /components/drawer
autoDemos: true
parent: 反馈
---

# Drawer 抽屉

有些时候, Dialog 组件并不满足我们的需求, 比如你的表单很长, 亦或是你需要临时展示一些文档, Drawer 拥有和 Dialog 几乎相同的 API, 在 UI 上带来不一样的体验。

## 用法

抽屉从父窗体边缘滑入，覆盖住部分父窗体内容，在不离开主路径的情况下，提供用户快速执行简单的操作、确认用户信息或反馈提示的辅助窗口。

{Demos}

### API

#### IDrawerProps

<Props propsName="IDrawerProps" omit={['fullWidth', 'fullScreen', 'disableSize']} />

#### ISlideAnimationProps

<Props propsName="ISlideAnimationProps" />
