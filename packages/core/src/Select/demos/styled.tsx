import { createGlobalStyle } from 'styled-components';

export const DocSelectStyle = createGlobalStyle`
  [class*='StyledSelectInputWrapper'] {
    margin-right: 12px;
  }
  [class*='Row'] {
    margin-bottom: 12px;
  }
`;
